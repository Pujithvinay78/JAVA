class practiceall{
    
    public static void main(String[] args) {
        String s="VikkU";
        System.out.println(s.length()+" "+s.charAt(2)+" "+s.toUpperCase()+" "+s.toLowerCase()+" "+s.substring(0,2)+" "+s.equals("Vikku")+" "+
        s.equalsIgnoreCase("Vikku")+" "+s.contains("Vik")+" "+s.trim()+" "+s.replace('U','I'));
    }
}